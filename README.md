<div align="center">
  <img src="src/app_image/pic.png" alt="Mika Rental Logo" width="150"/>
  <h1>Mika Rental - ระบบบริหารจัดการการเช่า-ยืม</h1>
  <p>โปรแกรม Desktop สำหรับบริหารจัดการสินค้าเช่า-ยืม พร้อมระบบสมาชิก, การคำนวณค่าบริการ, และการเชื่อมต่อ API ชำระเงิน</p>
</div>

---

## 📚 สารบัญ

- [คู่มือการใช้งานสำหรับผู้ใช้](#-คู่มือการใช้งานสำหรับผู้ใช้)
  - [ปุ่มลัด (Shortcut Keys)](#-ปุ่มลัด-shortcut-keys)
  - [ข้อมูลการล็อกอินพื้นฐาน](#-ข้อมูลการล็อกอินพื้นฐาน)
  - [การใช้งาน Console](#-การใช้งาน-console)
- [คู่มือสำหรับนักพัฒนา](#-คู่มือสำหรับนักพัฒนา)
  - [สิ่งที่ต้องมี (Prerequisites)](#-สิ่งที่ต้องมี-prerequisites)
  - [ขั้นตอนการติดตั้ง](#-ขั้นตอนการติดตั้ง)
  - [การรันโปรแกรม](#-การรันโปรแกรม)
  - [การ Build โปรแกรม](#-การ-build-โปรแกรม)
  - [การแพ็คซอร์สโค้ด](#-การแพ็คซอร์สโค้ด)

---

## 📖 คู่มือการใช้งานสำหรับผู้ใช้

### ⌨️ ปุ่มลัดเริ่มต้น (Default Shortcut Keys)

**หมายเหตุ:** ปุ่มลัดเหล่านี้เป็นค่าเริ่มต้นและสามารถเปลี่ยนแปลงได้ใน **หน้าต่างตั้งค่า Local > ปุ่มลัด**

| ปุ่ม | ฟังก์ชัน | รายละเอียด |
| :--- | :--- | :--- |
| **F1, F2, F3, etc.** | ฟังก์ชันต่างๆ | ดูรายละเอียดปุ่มลัดที่เป็นปัจจุบันได้ใน **หน้าต่างตั้งค่า > ข้อมูลโปรแกรม** |
| **~** | Console | เปิดหน้าต่าง Console สำหรับคำสั่งขั้นสูง |
| **F11** | Fullscreen | สลับโหมดการแสดงผลแบบเต็มหน้าจอ |

### 🔑 ข้อมูลการล็อกอินพื้นฐาน

โปรแกรมมีระบบล็อกอิน 2 ส่วนหลัก:

1.  **ผู้ใช้ทั่วไป (User)**:
    *   สามารถสมัครสมาชิกและเข้าสู่ระบบผ่านหน้าต่างหลักเพื่อทำการเช่า-ยืมสินค้า

2.  **ผู้ดูแลระบบ (Admin)**:
    *   **Admin Console (Local)**: ใช้สำหรับเข้า Console (`~`) และ Admin Panel (`F2`)
        *   **Username เริ่มต้น**: `admin` (สามารถเปลี่ยนได้ในการตั้งค่า Local)
        *   **Password เริ่มต้น**: `admin1234` (ต้องตั้งค่าครั้งแรกใน .ini หรือ หน้าต่างตั้งค่า Local)
    *   **Admin (Server)**: ใช้สำหรับเข้า Admin Panel (`F3`)
        *   **Username เริ่มต้น**: `admin`
        *   **Password เริ่มต้น**: `admin` (จะถูกสร้างขึ้นเมื่อใช้คำสั่ง `db init-server` ใน Console เป็นครั้งแรก)

### ⚙️ การใช้งาน Console

Console เป็นเครื่องมือสำหรับนักพัฒนาและผู้ดูแลระบบขั้นสูง สามารถเปิดได้โดยการกดปุ่มลัดที่ตั้งค่าไว้ (ค่าเริ่มต้นคือ **`~`** (Tilde))

**คำสั่งที่สำคัญ:**

| คำสั่ง | รายละเอียด |
| :--- | :--- |
| `help` หรือ `?` | แสดงรายการคำสั่งทั้งหมดที่มี |
| `db local` | สลับการทำงานของโปรแกรมไปใช้ฐานข้อมูล Local (SQLite) |
| `db server` | สลับการทำงานของโปรแกรมไปใช้ฐานข้อมูล Server (PostgreSQL/MySQL) |
| `db init-server` | **(สำคัญ)** สร้างตารางและข้อมูลเริ่มต้นที่จำเป็นทั้งหมดบนฐานข้อมูล Server |
| `admin` / `admin remote` | เปิดหน้า Admin Panel (Local / Server) |
| `setting` / `setting remote` | เปิดหน้าต่างการตั้งค่า (Local / Server) |
| `set theme <light/dark>` | เปลี่ยนธีมของโปรแกรม |
| `test scb` / `test ktb` / `test slipok` | ทดสอบการเชื่อมต่อ API ของธนาคารต่างๆ |
| `reset config` | **(ระวัง)** ลบไฟล์ `app_config.ini` และสร้างใหม่เป็นค่าเริ่มต้น |
| `reset superadmin` | **(ระวัง)** รีเซ็ตรหัสผ่านของ Super Admin (ID: 1) บน Server |
| `exit` หรือ `quit` | ปิดหน้าต่าง Console |

---

## 👨‍💻 คู่มือสำหรับนักพัฒนา

### ✅ สิ่งที่ต้องมี (Prerequisites)

1.  **Python** (แนะนำเวอร์ชัน 3.10 หรือสูงกว่า)
2.  **Poetry**:เครื่องมือสำหรับจัดการ Dependencies ของโปรเจกต์
    -   ติดตั้ง Poetry: ดูวิธีการติดตั้ง

### 🛠️ ขั้นตอนการติดตั้ง

1.  **Clone a repository**

    ```bash
    git clone https://github.com/your-username/Mika_Rental.git
    cd Mika_Rental
    ```

2.  **ติดตั้ง Dependencies ผ่าน Poetry**

    ใช้คำสั่ง `poetry install` เพื่อติดตั้งไลบรารีทั้งหมดที่ระบุไว้ในไฟล์ `poetry.lock` การทำเช่นนี้จะช่วยให้ทุกคนในทีมใช้ไลบรารีเวอร์ชันเดียวกัน

    ```bash
    poetry install
    ```

3.  **เปิดใช้งาน Virtual Environment**

    Poetry จะสร้าง Virtual Environment แยกสำหรับโปรเจกต์นี้โดยอัตโนมัติ ให้เปิดใช้งานด้วยคำสั่ง:

    ```bash
    poetry shell
    ```

    หลังจากรันคำสั่งนี้, Terminal ของคุณจะอยู่ในสภาพแวดล้อมของโปรเจกต์

4.  **(ถ้าจำเป็น) ติดตั้งไลบรารีเพิ่มเติมด้วย `pip`**

    ในกรณีที่มีบางไลบรารีที่ไม่สามารถติดตั้งผ่าน Poetry ได้ (ซึ่งเกิดขึ้นไม่บ่อย) คุณสามารถใช้ `pip` ภายใน `poetry shell` ได้โดยตรง:

    ```bash
    # ตรวจสอบให้แน่ใจว่าคุณอยู่ใน poetry shell ก่อน
    pip install some-package
    ```
    ```bash
    # 
    pip install cryptography 
    ```
    หรือรันผ่าน `poetry run`:

    ```bash
    poetry run pip install some-package
    ```

### 🚀 การรันโปรแกรม

หลังจากติดตั้ง Dependencies เรียบร้อยแล้ว สามารถรันโปรแกรมได้โดยใช้สคริปต์ `.bat` ที่เตรียมไว้ให้:

```bash
.\run_main.bat
```

หรือรันโดยตรงผ่าน Poetry:

```bash
poetry run python -m main
```

### 📦 การ Build โปรแกรม

สคริปต์ `build.bat` ใช้สำหรับสร้างไฟล์ `.exe` ของโปรแกรมด้วย PyInstaller

**วิธีใช้งาน:**

```bash
# Build แบบ one-folder (ค่าเริ่มต้น)
.\build.bat

# Build แบบ one-file
.\build.bat onefile

# Build และสร้าง Installer (ต้องติดตั้ง Inno Setup 6)
.\build.bat installer
.\build.bat onefile installer
```

ไฟล์ที่ได้จากการ Build จะอยู่ในโฟลเดอร์ `dist/`



### 🗜️ การแพ็คซอร์สโค้ด

หากต้องการบีบอัดไฟล์ซอร์สโค้ดทั้งหมดของโปรเจกต์ (ไม่รวมไฟล์ที่ไม่จำเป็น เช่น `.venv`, `__pycache__`) เพื่อนำไปใช้งานต่อ สามารถรันสคริปต์ `pack_project.py`

```bash
python pack_project.py
```

จะได้ไฟล์ `.zip` ที่มีชื่อว่า `Mika_Rental_Source_YYYYMMDD_HHMMSS.zip` อยู่ในโฟลเดอร์หลักของโปรเจกต์
